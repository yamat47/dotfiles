dotfiles
==========

usage
---------------------------

### homeディレクトリに`.config`ディレクトリを作る

```
$ cd ~
$ mkdir .config
```

### `config`ディレクトリ下にリポジトリを持ってくる

```
$ cd .config
$ git clone git@gitlab.com:yamat47/dotfiles.git
```

### `install.sh`を実行

```
$ sh install.sh
```

### terminalを再起動
